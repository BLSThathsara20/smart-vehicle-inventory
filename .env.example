# Supabase - Get these from your Supabase Dashboard > Project Settings > API
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Database password (for server-side/direct DB connections only - NEVER expose in client)
# Not used by the Supabase JS client - client uses anon key above
SUPABASE_DB_PASSWORD=your-db-password
